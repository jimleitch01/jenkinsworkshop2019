// EX04 Shell & Variables

pipeline {
    agent any

    stages {
        stage('Stage One') {
            steps {
                sh 'echo My Build Number is: ${BUILD_NUMBER}'
                sh 'echo My Job name is: ${JOB_NUMBER}' 
                sh 'echo My Workspace is: ${WORKSPACE}' 
            }

        }
    }
}

